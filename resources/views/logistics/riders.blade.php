@extends('layouts.logistics') @section('title','Riders / Couriers') @section('content')
<div class="log-page-head"><div><h1>Riders / Couriers</h1><p>Manage approved SHOPPICK Riders, availability, assignments, documents, and delivery activity.</p></div><button class="btn-primary btn-sm" onclick="document.getElementById('add-rider').showModal()">+ Add Rider</button></div>
<nav class="mt-5 flex gap-2 overflow-x-auto pb-2" aria-label="Approved Rider status filters">@foreach([''=>'All Riders','available'=>'Available','assigned'=>'Assigned','on_pickup'=>'On Pickup','on_delivery'=>'On Delivery','off_duty'=>'Off Duty','unavailable'=>'Unavailable','suspended'=>'Suspended'] as $key=>$label)<a href="{{ route('logistics.riders',['status'=>$key?:null]) }}" class="whitespace-nowrap rounded-full px-4 py-2 text-sm font-semibold {{ request('status','')===$key?'bg-brand-600 text-white':'border bg-white text-slate-600' }}">{{ $label }} <span class="ml-1 opacity-75">{{ $filterCounts[$key] }}</span></a>@endforeach</nav>
<div class="log-kpis">@foreach(['Active Riders'=>$stats['active'],'Available'=>$stats['available'],'On Pickup'=>$stats['pickup'],'Assigned / On Delivery'=>$stats['delivery'],'Unavailable'=>$stats['unavailable'],'License Attention'=>$stats['license_attention']] as $label=>$value)<div class="log-kpi"><b>{{ $value }}</b><p>{{ $label }}</p></div>@endforeach</div>
<form class="log-filters"><input type="hidden" name="status" value="{{ request('status') }}"><input class="input flex-1" name="q" value="{{ request('q') }}" placeholder="Search name, email, mobile, or vehicle"><select class="input" name="account_status"><option value="">All Accounts</option>@foreach(['active','inactive','suspended'] as $s)<option value="{{ $s }}" @selected(request('account_status')===$s)>{{ str($s)->headline() }}</option>@endforeach</select><select class="input" name="availability"><option value="">All Availability</option>@foreach(['available','assigned','on_pickup','on_delivery','off_duty','unavailable'] as $s)<option value="{{ $s }}" @selected(request('availability')===$s)>{{ str($s)->headline() }}</option>@endforeach</select><select class="input" name="hub_id"><option value="">All Sorting Centers</option>@foreach($hubs as $hub)<option value="{{ $hub->id }}" @selected(request('hub_id')==$hub->id)>{{ $hub->name }}</option>@endforeach</select><select class="input" name="license_status"><option value="">All License Statuses</option>@foreach(['verified','pending_review','rejected'] as $license)<option value="{{ $license }}" @selected(request('license_status')===$license)>{{ str($license)->headline() }}</option>@endforeach</select><button class="btn-primary btn-sm">Apply</button></form>
<div class="mt-4 grid gap-3 xl:grid-cols-2">@forelse($riders as $profile)@php($job=$currentAssignments->first(fn($shipment)=>$shipment->rider_id===$profile->user_id||$shipment->pickup_rider_id===$profile->user_id))<article class="log-card p-4"><div class="flex items-start justify-between gap-3"><div><b>{{ $profile->user->name }}</b><small class="block">{{ $profile->user->email }} · {{ $profile->user->phone }}</small></div><x-admin.status-badge :status="$profile->account_status"/></div><div class="mt-3 grid grid-cols-2 gap-3 text-sm"><div><span class="text-slate-400">Availability</span><p class="font-semibold">{{ str($profile->availability)->headline() }}</p></div><div><span class="text-slate-400">Current Assignment</span><p>{{ $job?->parcel_code?:$job?->shipment_number?:'None' }}</p></div><div><span class="text-slate-400">Vehicle</span><p>{{ $job?->vehicle?->code?:$profile->vehicle?->code?:'Unassigned' }}</p></div><div><span class="text-slate-400">Sorting Center</span><p>{{ $profile->hub?->name?:'Unassigned' }}</p></div><div><span class="text-slate-400">Driver License</span><p class="font-semibold {{ in_array($profile->license_display_status,['expired','rejected'])?'text-rose-700':($profile->license_display_status==='verified'?'text-brand-700':'text-amber-700') }}">{{ str($profile->license_display_status)->headline() }}</p></div>@if($job)<div><span class="text-slate-400">Job Status / Area</span><p>{{ str($job->status)->headline() }} · {{ $job->deliveryArea?->name?:$job->store?->name }}</p></div>@endif</div><div class="mt-4 flex flex-wrap gap-2"><a href="{{ request()->fullUrlWithQuery(['view'=>$profile->id]) }}" class="btn-outline btn-sm inline-flex">View Rider</a>@if($job)<a href="{{ route('logistics.shipments.show',$job) }}" class="btn-primary btn-sm inline-flex">{{ $profile->availability==='on_pickup'?'View Pickup':'View Delivery' }}</a>@endif</div></article>@empty<p class="log-empty xl:col-span-2">No approved Riders match these filters.</p>@endforelse</div><div class="mt-4">{{ $riders->links() }}</div>
<dialog id="add-rider" class="m-auto max-h-[92vh] w-full max-w-3xl overflow-y-auto rounded-2xl p-0 backdrop:bg-navy-900/50"><form method="POST" enctype="multipart/form-data" action="{{ route('logistics.riders.store') }}" class="space-y-5 p-6">@csrf<div class="flex justify-between"><div><h2 class="font-bold">Add Rider</h2><p class="text-xs text-slate-500">License review is required before assignments.</p></div><button type="button" onclick="this.closest('dialog').close()">×</button></div>
<h3 class="font-bold">Personal Information</h3><div class="grid gap-3 sm:grid-cols-3"><input class="input" name="first_name" required placeholder="First Name"><input class="input" name="middle_initial" placeholder="Middle Initial"><input class="input" name="last_name" required placeholder="Last Name"></div><div class="grid gap-3 sm:grid-cols-3"><select class="input" name="sex" required><option value="">Sex</option><option value="female">Female</option><option value="male">Male</option></select><x-birthday-age-fields /></div><div class="grid gap-3 sm:grid-cols-2"><input class="input" type="email" name="email" required placeholder="Email"><input class="input" name="phone" required placeholder="Mobile Number"></div>
<h3 class="font-bold">Address</h3><input class="input" name="address_line" required placeholder="House No. / Street"><x-philippine-location-fields/><input class="input" name="postal_code" required placeholder="Postal Code">
<fieldset class="space-y-3 rounded-xl border p-4"><legend class="px-2 font-bold">Rider Documents</legend><div class="grid gap-3 sm:grid-cols-2"><label class="text-sm font-semibold">License Number *<input class="input mt-1" name="driver_license_number" maxlength="80" required></label><label class="text-sm font-semibold">Classification *<input class="input mt-1" name="driver_license_classification" maxlength="100" required></label><label class="text-sm font-semibold">Expiration Date *<input class="input mt-1" type="date" name="driver_license_expires_at" required></label></div><div class="grid gap-3 sm:grid-cols-2"><label class="text-sm font-semibold">License Front *<input class="input mt-1" type="file" name="driver_license_front" accept=".pdf,.jpg,.jpeg,.png" capture="environment" required><small class="block font-normal text-slate-500">Take a photo or upload a file.</small></label><label class="text-sm font-semibold">License Back *<input class="input mt-1" type="file" name="driver_license_back" accept=".pdf,.jpg,.jpeg,.png" capture="environment" required><small class="block font-normal text-slate-500">Stored privately.</small></label></div></fieldset>
<h3 class="font-bold">Rider Operations & Account</h3><div class="grid gap-3 sm:grid-cols-2"><input class="input" name="emergency_contact_name" placeholder="Emergency Contact"><input class="input" name="emergency_contact_phone" placeholder="Emergency Contact Phone"><select class="input" name="hub_id"><option value="">Sorting Center / Hub</option>@foreach($hubs as $hub)<option value="{{ $hub->id }}">{{ $hub->name }}</option>@endforeach</select><select class="input" name="vehicle_id"><option value="">Official Vehicle</option>@foreach($vehicles as $vehicle)<option value="{{ $vehicle->id }}">{{ $vehicle->code }} · {{ $vehicle->plate_number }}</option>@endforeach</select><input class="input" type="password" name="password" required placeholder="Temporary Password"><input class="input" type="password" name="password_confirmation" required placeholder="Confirm Password"></div><button class="btn-primary min-h-11 w-full">Create Rider for Review</button></form></dialog>
@if($selected)<aside class="log-drawer"><div class="log-drawer-head"><h2>{{ $selected->user->name }}</h2><a href="{{ route('logistics.riders',request()->except('view')) }}">×</a></div><section class="log-drawer-section"><h3>Profile</h3><p class="text-sm">{{ $selected->user->email }}<br>{{ $selected->user->phone }}<br>{{ $selected->user->defaultAddress()?->full_address }}</p></section>
<section class="log-drawer-section"><h3>Documents</h3><dl class="grid grid-cols-2 gap-2 text-sm"><div><dt class="text-slate-400">License Number</dt><dd>{{ $selected->driver_license_number?:'Not recorded' }}</dd></div><div><dt class="text-slate-400">Classification</dt><dd>{{ $selected->driver_license_classification?:'Not recorded' }}</dd></div><div><dt class="text-slate-400">Expiration</dt><dd>{{ $selected->driver_license_expires_at?->format('M d, Y')?:'Not recorded' }}</dd></div><div><dt class="text-slate-400">Status</dt><dd class="font-semibold">{{ str($selected->license_display_status)->headline() }}</dd></div><div><dt class="text-slate-400">Reviewed By</dt><dd>{{ $selected->licenseReviewer?->name?:'Not reviewed' }}</dd></div><div><dt class="text-slate-400">Reviewed At</dt><dd>{{ $selected->driver_license_reviewed_at?->format('M d, Y g:i A')?:'—' }}</dd></div></dl><div class="mt-3 flex gap-2">@foreach(['front'=>'Front Document','back'=>'Back Document'] as $side=>$label)@if(data_get($selected,"driver_license_{$side}_path"))<a class="btn-outline btn-sm" href="{{ route('logistics.riders.license.file',[$selected,$side]) }}">{{ $label }}</a>@endif @endforeach</div>
<form method="POST" enctype="multipart/form-data" action="{{ route('logistics.riders.license.update',$selected) }}" class="mt-4 space-y-2">@csrf @method('PUT')<input class="input" name="driver_license_number" value="{{ $selected->driver_license_number }}" required placeholder="License number"><input class="input" name="driver_license_classification" value="{{ $selected->driver_license_classification }}" required placeholder="Classification"><input class="input" type="date" name="driver_license_expires_at" value="{{ $selected->driver_license_expires_at?->format('Y-m-d') }}" required><label class="text-xs">Replace front<input class="input mt-1" type="file" name="driver_license_front" accept=".pdf,.jpg,.jpeg,.png"></label><label class="text-xs">Replace back<input class="input mt-1" type="file" name="driver_license_back" accept=".pdf,.jpg,.jpeg,.png"></label><button class="btn-outline btn-sm">Save & Return to Review</button></form>
<form method="POST" action="{{ route('logistics.riders.license.review',$selected) }}" class="mt-4 space-y-2">@csrf<select class="input" name="status"><option value="verified">Verify License</option><option value="rejected">Reject License</option></select><textarea class="input" name="reason" placeholder="Rejection reason (required when rejected)"></textarea><button class="btn-primary btn-sm">Save Review</button></form>@if($selected->licenseAudits->isNotEmpty())<h4 class="mt-4 text-xs font-bold uppercase">Audit History</h4>@foreach($selected->licenseAudits->sortByDesc('created_at') as $audit)<p class="mt-2 text-xs"><b>{{ str($audit->event)->headline() }}</b> · {{ $audit->actor?->name?:'System' }}<br><span class="text-slate-500">{{ $audit->created_at->format('M d, Y g:i A') }} {{ $audit->reason?'· '.$audit->reason:'' }}</span></p>@endforeach @endif</section>
<section class="log-drawer-section"><h3>Operations</h3><form method="POST" action="{{ route('logistics.riders.update',$selected) }}" class="space-y-2">@csrf @method('PUT')<select class="input" name="account_status">@foreach(['active','inactive','suspended'] as $s)<option value="{{ $s }}" @selected($selected->account_status===$s)>{{ str($s)->headline() }}</option>@endforeach</select><select class="input" name="availability">@foreach(['available','assigned','on_pickup','on_delivery','off_duty','unavailable'] as $s)<option value="{{ $s }}" @selected($selected->availability===$s)>{{ str($s)->headline() }}</option>@endforeach</select><select class="input" name="hub_id"><option value="">No Hub</option>@foreach($hubs as $hub)<option value="{{ $hub->id }}" @selected($selected->hub_id===$hub->id)>{{ $hub->name }}</option>@endforeach</select><select class="input" name="vehicle_id"><option value="">No Vehicle</option>@foreach($vehicles as $vehicle)<option value="{{ $vehicle->id }}" @selected($selected->vehicle_id===$vehicle->id)>{{ $vehicle->code }}</option>@endforeach</select><input class="input" name="emergency_contact_name" value="{{ $selected->emergency_contact_name }}" placeholder="Emergency Contact"><input class="input" name="emergency_contact_phone" value="{{ $selected->emergency_contact_phone }}" placeholder="Emergency Phone"><textarea class="input" name="notes">{{ $selected->notes }}</textarea><button class="btn-primary">Save Rider</button></form></section>
<section class="log-drawer-section"><h3>Assigned Areas</h3><form method="POST" action="{{ route('logistics.riders.areas',$selected) }}">@csrf @method('PUT')@foreach($deliveryAreas as $area)<label class="block py-1 text-sm"><input type="checkbox" name="delivery_area_ids[]" value="{{ $area->id }}" @checked($selected->deliveryAreas->contains($area))> {{ $area->name }}</label>@endforeach<button class="btn-outline btn-sm mt-2">Save Areas</button></form></section>
<section class="log-drawer-section"><h3>Jobs / POD / COD</h3>@forelse($selectedShipments as $shipment)<div class="border-t py-3 text-sm"><b>{{ $shipment->parcel_code?:$shipment->shipment_number }}</b> · {{ str($shipment->status)->headline() }}@if($shipment->proofOfDelivery)<p>POD: {{ str($shipment->proofOfDelivery->status)->headline() }}</p>@endif @php($cod=$shipment->order?->payments->where('method','cod')->sortByDesc('id')->first()) @if($cod?->remittance_status==='pending')<form method="POST" action="{{ route('logistics.cod.remit',$cod) }}" class="mt-2">@csrf<button class="btn-primary btn-sm">Confirm ₱{{ number_format($cod->amount,2) }} Remitted</button></form>@endif</div>@empty<p class="text-sm text-slate-400">No history.</p>@endforelse</section><section class="log-drawer-section"><h3>Message Rider</h3><form method="POST" action="{{ route('logistics.riders.messages',$selected) }}" class="space-y-2">@csrf<textarea class="input" name="message" required></textarea><button class="btn-outline btn-sm">Send Message</button></form></section></aside>@endif @endsection
