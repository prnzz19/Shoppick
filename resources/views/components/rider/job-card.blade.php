@props(['shipment'])
@php($pickup=$shipment->pickup_rider_id===auth()->id()&&!$shipment->pickup_arrived_at)
<article class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div class="flex items-center justify-between gap-3"><span class="rounded-full bg-brand-50 px-3 py-1 text-xs font-bold text-brand-700">{{ $pickup?'PICKUP':'DELIVERY' }}</span><x-admin.status-badge :status="$shipment->status"/></div><h3 class="mt-4 text-lg font-black">{{ $shipment->parcel_code?:$shipment->shipment_number }}</h3><dl class="mt-3 grid gap-2 text-sm"><div><dt class="text-slate-400">{{ $pickup?'Shop':'Delivery Area' }}</dt><dd class="font-semibold">{{ $pickup?($shipment->store?->name??'Shop'):($shipment->deliveryArea?->name??collect($shipment->delivery_address)->filter()->implode(', ')) }}</dd></div><div><dt class="text-slate-400">Payment</dt><dd class="font-semibold">{{ $shipment->order?->payment_method==='cod'?'COD ₱'.number_format($shipment->order->total,2):'Paid Online' }}</dd></div></dl><a href="{{ route('rider.shipments.show',$shipment) }}" class="btn-primary mt-4 flex min-h-11 w-full items-center justify-center">View Job</a></article>
