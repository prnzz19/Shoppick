@extends('layouts.storefront') @section('title',$store->name) @section('content')<section class="bg-navy-900 text-white"><div class="mx-auto max-w-7xl px-4 py-10"><div class="flex items-center gap-5">@if($store->logo)<img src="{{ asset('storage/'.$store->logo) }}" class="h-20 w-20 rounded-2xl object-cover">@endif<div><h1 class="text-3xl font-black">{{ $store->name }}</h1><p class="mt-2 text-slate-300">{{ $store->description }}</p><p class="mt-2 text-sm">★ {{ number_format($store->rating_avg,1) }} · {{ $store->products()->active()->count() }} products</p></div></div></div></section><div class="mx-auto max-w-7xl px-4 py-10"><div class="grid grid-cols-2 gap-4 md:grid-cols-4 lg:grid-cols-5">@foreach($products as $product)<x-product-card :product="$product"/>@endforeach</div><div class="mt-6">{{ $products->links() }}</div></div>@endsection
