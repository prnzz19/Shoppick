@extends('layouts.rider') @section('title',$type==='pickup'?'Pickup Jobs':($type==='delivery'?'Delivery Jobs':'My Jobs')) @section('content')
<div class="flex gap-2 overflow-x-auto pb-2">@foreach([''=>'All','assigned'=>'Assigned','active'=>'Active','completed'=>'Completed'] as $key=>$label)<a href="{{ request()->fullUrlWithQuery(['status'=>$key?:null]) }}" class="whitespace-nowrap rounded-full px-4 py-2 text-sm font-semibold {{ request('status','')===$key?'bg-brand-600 text-white':'border bg-white text-slate-600' }}">{{ $label }}</a>@endforeach</div><div class="mt-4 grid gap-4 md:grid-cols-2">@forelse($shipments as $shipment)<x-rider.job-card :shipment="$shipment"/>@empty<div class="rounded-2xl border border-dashed bg-white p-10 text-center text-slate-500 md:col-span-2">No jobs match this view.</div>@endforelse</div><div class="mt-5">{{ $shipments->links() }}</div>@endsection
