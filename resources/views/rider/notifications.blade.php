@extends('layouts.rider') @section('title','Notifications') @section('content')<form method="POST" action="{{ route('rider.notifications.read-all') }}" class="mb-4 text-right">@csrf<button class="btn-outline btn-sm">Mark all read</button></form><div class="space-y-3">@forelse($items as $item)<a href="{{ route('rider.notifications.open',$item) }}" class="block rounded-2xl border bg-white p-4 {{ $item->read_at?'':'border-brand-300' }}"><b>{{ $item->title }}</b><p class="mt-1 text-sm text-slate-600">{{ $item->body }}</p><p class="mt-2 text-xs text-slate-400">{{ $item->created_at->diffForHumans() }}</p></a>@empty<p class="rounded-2xl border bg-white p-8 text-center text-slate-500">No notifications.</p>@endforelse</div><div class="mt-5">{{ $items->links() }}</div>@endsection
