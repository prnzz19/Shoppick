@extends('layouts.rider') @section('title','Job Details') @section('content')
@php($pickup=$shipment->pickup_rider_id===auth()->id()&&!$shipment->pickup_arrived_at) @php($cod=$shipment->order->payment_method==='cod') @php($payment=$shipment->order->payments->where('method','cod')->sortByDesc('id')->first())
<a href="{{ route('rider.jobs') }}" class="text-sm font-semibold text-brand-700">← My Jobs</a><div class="mt-3 flex items-start justify-between gap-3"><div><span class="text-xs font-bold text-brand-600">{{ $pickup?'PICKUP':'DELIVERY' }}</span><h1 class="text-2xl font-black">{{ $shipment->parcel_code?:$shipment->shipment_number }}</h1></div><x-admin.status-badge :status="$shipment->status"/></div>
<section class="mt-5 grid gap-4 md:grid-cols-2"><div class="rounded-2xl border bg-white p-5"><h2 class="font-bold">{{ $pickup?'Pickup Information':'Delivery Information' }}</h2><dl class="mt-3 space-y-3 text-sm">@if($pickup)<div><dt class="text-slate-400">Shop</dt><dd class="font-semibold">{{ $shipment->store?->name }}</dd></div><div><dt class="text-slate-400">Seller Pickup Address</dt><dd>{{ data_get($shipment->pickup_address,'address','Not recorded') }}</dd></div><div><dt class="text-slate-400">Seller Contact</dt><dd>{{ $shipment->store?->user?->phone?:'Contact Logistics' }}</dd></div><div><dt class="text-slate-400">Sorting Center</dt><dd>{{ $shipment->currentHub?->name?:auth()->user()->riderProfile?->hub?->name?:'SHOPPICK Sorting Center' }}</dd></div>@else<div><dt class="text-slate-400">Buyer</dt><dd class="font-semibold">{{ $shipment->order->buyer_name?:$shipment->order->user?->name }}</dd></div><div><dt class="text-slate-400">Delivery Address</dt><dd>{{ collect($shipment->delivery_address)->filter()->implode(', ') }}</dd></div><div><dt class="text-slate-400">Contact</dt><dd>{{ $shipment->order->buyer_phone?:data_get($shipment->delivery_address,'phone') }}</dd></div>@endif<div><dt class="text-slate-400">Items</dt><dd>{{ $shipment->sellerOrder->items->sum('quantity') }} item(s)</dd></div></dl><div class="mt-4 grid gap-2 sm:grid-cols-2">@php($map=$pickup?data_get($shipment->pickup_address,'address'):collect($shipment->delivery_address)->filter()->implode(', ')) @if($map)<a target="_blank" rel="noopener" href="https://www.google.com/maps/search/?api=1&query={{ urlencode($map) }}" class="btn-outline flex min-h-11 items-center justify-center">Open in Maps</a>@endif @if($pickup&&$shipment->currentHub?->address)<a target="_blank" rel="noopener" href="https://www.google.com/maps/search/?api=1&query={{ urlencode($shipment->currentHub->address) }}" class="btn-outline flex min-h-11 items-center justify-center">Open Hub in Maps</a>@endif</div></div>
<div class="rounded-2xl border bg-white p-5"><h2 class="font-bold">Payment</h2>@if($cod)<p class="mt-3 text-xs font-bold tracking-wider text-amber-700">CASH ON DELIVERY</p><p class="mt-1 text-2xl font-black">₱{{ number_format($shipment->order->total,2) }}</p><p class="text-sm text-slate-500">Amount to collect</p><x-admin.status-badge class="mt-3" :status="$payment?->status?:'pending'"/>@if($shipment->status==='out_for_delivery'&&!in_array($payment?->status,['cod_collected','paid']))<form method="POST" action="{{ route('rider.shipments.cod-collection',$shipment) }}" class="mt-4" data-confirm-title="Confirm COD Collection" data-confirm-message="Confirm that ₱{{ number_format($shipment->order->total,2) }} was received from the Buyer." data-confirm-action="Confirm Collection" data-confirm-type="success">@csrf<button class="btn-primary min-h-11 w-full">COD Collected</button></form>@endif @else<p class="mt-3 font-semibold">Paid Online</p><p class="text-sm text-slate-500">Do not collect cash from the Buyer.</p>@endif</div></section>
<section class="mt-5 rounded-2xl border bg-white p-5"><h2 class="font-bold">Timeline</h2><div class="mt-4 border-l-2 border-brand-100 pl-4">@forelse($shipment->events->sortByDesc('created_at') as $event)<div class="pb-4"><b class="text-sm">{{ str($event->status)->headline() }}</b><p class="text-sm text-slate-500">{{ $event->note }}</p><p class="text-xs text-slate-400">{{ $event->created_at->format('M d, Y · g:i A') }}</p></div>@empty<p class="text-sm text-slate-500">No activity yet.</p>@endforelse</div></section>
@if(!$pickup&&$shipment->status==='out_for_delivery')<form method="POST" enctype="multipart/form-data" action="{{ route('rider.shipments.pod',$shipment) }}" class="mt-5 space-y-3 rounded-2xl border bg-white p-5">@csrf<h2 class="font-bold">Proof of Delivery</h2><input class="input" name="recipient_name" value="{{ old('recipient_name',$shipment->proofOfDelivery?->recipient_name) }}" required placeholder="Recipient name"><label class="block text-sm font-semibold">Delivery photo *<input class="input mt-1" type="file" name="photo" accept="image/*" capture="environment" @required(!$shipment->proofOfDelivery)></label><textarea class="input" name="notes" placeholder="Delivery note">{{ old('notes',$shipment->proofOfDelivery?->notes) }}</textarea><button class="btn-primary min-h-11 w-full">{{ $shipment->proofOfDelivery?'Update':'Submit' }} POD</button></form>@endif
@if($shipment->status==='out_for_delivery')<details class="mt-5 rounded-2xl border bg-white p-5"><summary class="cursor-pointer font-semibold text-rose-700">Report Delivery Problem</summary><form method="POST" action="{{ route('rider.shipments.parcel-action',$shipment) }}" class="mt-4 space-y-3">@csrf<input type="hidden" name="action" value="failed"><select name="reason" class="input" required><option value="">Select reason</option>@foreach(['recipient_unavailable','incorrect_address','reschedule_requested','parcel_issue','payment_issue','other'] as $reason)<option value="{{ $reason }}">{{ str($reason)->headline() }}</option>@endforeach</select><textarea name="details" class="input" placeholder="Additional details"></textarea><button class="min-h-11 w-full rounded-xl bg-rose-700 px-4 font-bold text-white">Notify Logistics</button></form></details>@endif
@php($action=match($shipment->status){'pickup_assigned'=>['accept_pickup','Accept Pickup'],'pickup_accepted'=>['confirm_pickup','Mark as Picked Up'],'picked_up'=>!$shipment->pickup_arrived_at?['arrive_sorting','Arrived at Sorting Center']:null,'assigned_to_rider'=>!$shipment->delivery_accepted_at?['accept_delivery','Accept Delivery']:['start_delivery','Start Delivery'],'out_for_delivery'=>$shipment->proofOfDelivery&&(!$cod||in_array($payment?->status,['cod_collected','paid']))?['delivered','Complete Delivery']:null,default=>null})
@if($action)<div class="rider-sticky-action"><form method="POST" action="{{ route('rider.shipments.parcel-action',$shipment) }}" class="mx-auto flex max-w-3xl gap-2">@csrf<input type="hidden" name="action" value="{{ $action[0] }}">@if($action[0]==='confirm_pickup')<input class="input" name="parcel_code" required placeholder="Parcel code">@endif<button class="btn-primary min-h-12 flex-1">{{ strtoupper($action[1]) }}</button></form>@if(in_array($shipment->status,['pickup_assigned','assigned_to_rider']))<form method="POST" action="{{ route('rider.shipments.decline',$shipment) }}" class="mx-auto mt-2 flex max-w-3xl gap-2">@csrf<select name="reason" class="input" required><option value="">Can't accept — reason</option><option value="vehicle_issue">Vehicle Issue</option><option value="unavailable">Unavailable</option><option value="emergency">Emergency</option><option value="other">Other</option></select><button class="btn-outline min-h-11">Can't Accept</button></form>@endif</div>@endif
@endsection
