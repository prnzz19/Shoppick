@extends('layouts.rider') @section('title','Chat / Support') @section('content')<div class="rounded-2xl border bg-white p-4"><p class="text-sm text-slate-500">Message SHOPPICK Logistics about assignments, parcels, or delivery problems.</p><div class="mt-4 max-h-[50vh] space-y-3 overflow-y-auto">@forelse($messages->reverse() as $message)<div class="max-w-[85%] rounded-2xl p-3 text-sm {{ $message->sender_id===auth()->id()?'ml-auto bg-brand-600 text-white':'bg-slate-100' }}"><b class="block text-xs opacity-75">{{ $message->sender_id===auth()->id()?'You':'SHOPPICK Logistics' }}</b>{{ $message->message }}</div>@empty<p class="py-8 text-center text-sm text-slate-400">No messages yet.</p>@endforelse</div><form method="POST" action="{{ route('rider.messages.send') }}" class="mt-4 flex gap-2">@csrf<textarea name="message" rows="2" required class="input" placeholder="Type your message"></textarea><button class="btn-primary min-h-11">Send</button></form></div><div class="mt-5">{{ $messages->links() }}</div>@endsection
