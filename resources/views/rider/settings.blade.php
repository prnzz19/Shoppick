@extends('layouts.rider') @section('title','Settings') @section('content')<div class="grid gap-5 lg:grid-cols-2"><form method="POST" action="{{ route('rider.settings.update') }}" class="space-y-3 rounded-2xl border bg-white p-5">@csrf @method('PUT')<h2 class="font-bold">Notification Preferences</h2>@foreach(['assignments'=>'Assignment updates','status'=>'Status and POD updates','support'=>'Support messages'] as $key=>$label)<label class="flex min-h-11 items-center gap-3"><input type="checkbox" name="{{ $key }}" value="1" @checked($preferences[$key]??true)>{{ $label }}</label>@endforeach<button class="btn-primary min-h-11 w-full">Save Preferences</button></form><form method="POST" action="{{ route('rider.password.update') }}" class="space-y-3 rounded-2xl border bg-white p-5">@csrf @method('PUT')<h2 class="font-bold">Change Password</h2><input class="input" type="password" name="current_password" required placeholder="Current password"><input class="input" type="password" name="password" required placeholder="New password"><input class="input" type="password" name="password_confirmation" required placeholder="Confirm new password"><button class="btn-primary min-h-11 w-full">Change Password</button></form></div>@endsection
