@extends('layouts.rider') @section('title','Proof of Delivery') @section('content')<div class="grid gap-4">@forelse($pods as $pod)<article class="rounded-2xl border bg-white p-5"><div class="flex justify-between gap-3"><div><b>{{ $pod->shipment->parcel_code?:$pod->shipment->shipment_number }}</b><p class="text-sm text-slate-500">{{ $pod->shipment->store?->name }} · {{ $pod->recipient_name }}</p></div><x-admin.status-badge :status="$pod->status"/></div><p class="mt-3 text-xs text-slate-400">Submitted {{ $pod->submitted_at?->format('M d, Y · g:i A') }}</p>@if($pod->review_notes)<p class="mt-2 text-sm text-rose-700">{{ $pod->review_notes }}</p>@endif @if($pod->photo_path)<a class="btn-outline btn-sm mt-3 inline-flex" href="{{ route('rider.pods.file',$pod) }}">Download authorized photo</a>@endif</article>@empty<div class="rounded-2xl border border-dashed bg-white p-10 text-center text-slate-500">No Proof of Delivery records yet.</div>@endforelse</div><div class="mt-5">{{ $pods->links() }}</div>@endsection
