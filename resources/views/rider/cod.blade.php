@extends('layouts.rider') @section('title','COD / Earnings') @section('content')<div class="rounded-2xl border border-amber-200 bg-amber-50 p-5"><p class="text-xs font-bold text-amber-700">COD TO REMIT</p><b class="mt-1 block text-3xl">₱{{ number_format($pending,2) }}</b><p class="mt-2 text-sm text-slate-600">Logistics must confirm official remittance.</p></div><div class="mt-5 space-y-3">@forelse($payments as $payment)<article class="flex items-center justify-between rounded-2xl border bg-white p-4"><div><b>{{ $payment->order->order_number }}</b><p class="text-xs text-slate-500">Collected {{ $payment->collected_at?->format('M d, Y · g:i A') }}</p></div><div class="text-right"><b>₱{{ number_format($payment->amount,2) }}</b><p class="text-xs font-semibold {{ $payment->remittance_status==='remitted'?'text-brand-700':'text-amber-700' }}">{{ str($payment->remittance_status?:'pending')->headline() }}</p></div></article>@empty<p class="rounded-2xl border bg-white p-8 text-center text-slate-500">No COD collections.</p>@endforelse</div><div class="mt-5">{{ $payments->links() }}</div><div class="mt-6 rounded-xl bg-slate-100 p-4 text-sm text-slate-600"><b>Rider earnings tracking is not configured.</b><p>COD records do not add a charge to the Buyer.</p></div>@endsection
