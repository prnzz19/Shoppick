@extends('layouts.storefront') @section('title','Rider Dashboard') @section('content')<div class="mx-auto max-w-5xl px-4 py-8"><h1 class="text-2xl font-black">Rider Dashboard</h1><p class="text-sm text-slate-500">Your assigned SHOPPICK deliveries.</p><div class="mt-5 space-y-3">@forelse($shipments as $s)<a href="{{ route('rider.shipments.show',$s) }}" class="card flex justify-between p-4"><div><b>{{ $s->shipment_number }}</b><p class="text-sm text-slate-500">{{ $s->store?->name }} → {{ data_get($s->delivery_address,'city','Buyer') }}</p></div><x-admin.status-badge :status="$s->status"/></a>@empty<div class="card p-10 text-center text-slate-500">No assigned deliveries.</div>@endforelse</div></div>@endsection
