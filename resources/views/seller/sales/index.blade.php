@extends('layouts.seller') @section('title','Sales') @section('content')<div><p class="text-sm font-semibold text-brand-600">Performance</p><h1 class="text-2xl font-extrabold text-navy-900">Sales & Earnings</h1><p class="text-sm text-slate-500">Real totals from your store’s seller orders.</p></div><div class="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-5">@foreach(['Gross Sales'=>$stats['gross'],'Completed Earnings'=>$stats['completed'],'Pending Earnings'=>$stats['pending'],'Cancelled Sales'=>$stats['cancelled'],'Platform Commission'=>$stats['commission']] as $label=>$value)<div class="card p-5"><p class="text-xs font-semibold uppercase text-slate-500">{{ $label }}</p><p class="mt-2 text-xl font-black text-navy-900">₱{{ number_format($value,2) }}</p></div>@endforeach</div><div class="card mt-6 overflow-x-auto p-5"><h2 class="font-bold">Transaction Summary</h2><table class="mt-4 w-full min-w-[650px] text-left text-sm"><thead><tr class="border-b text-xs uppercase text-slate-400"><th class="py-3">Seller Order</th><th>Date</th><th>Status</th><th>Gross</th><th>Commission</th><th>Net</th></tr></thead><tbody>@foreach($orders as $order)<tr class="border-b"><td class="py-4"><a class="font-semibold text-brand-600" href="{{ route('seller.orders.show',$order) }}">{{ $order->seller_order_number }}</a></td><td>{{ $order->created_at->format('M d, Y') }}</td><td><x-admin.status-badge :status="$order->status"/></td><td>₱{{ number_format($order->subtotal,2) }}</td><td>₱{{ number_format($order->commission_amount,2) }}</td><td class="font-bold">₱{{ number_format($order->seller_total,2) }}</td></tr>@endforeach</tbody></table></div><div class="mt-5">{{ $orders->links() }}</div>@endsection
