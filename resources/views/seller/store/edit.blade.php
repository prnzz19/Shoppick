@extends('layouts.seller') @section('title','Store Settings') @section('content')
<div><p class="text-sm font-semibold text-brand-600">Store Profile</p><h1 class="text-2xl font-extrabold text-navy-900">Store Settings</h1><p class="text-sm text-slate-500">Keep your public SHOPPICK store accurate and recognizable.</p></div>
<form method="POST" action="{{ route('seller.store.update') }}" enctype="multipart/form-data" class="card mt-6 p-6">@csrf @method('PUT')
<div class="grid gap-6 lg:grid-cols-3"><div class="lg:col-span-2 space-y-4"><div><label class="label">Store Name</label><input class="input" name="name" value="{{ old('name',$store->name) }}" required></div><div><label class="label">Store Description</label><textarea class="input" rows="5" name="description">{{ old('description',$store->description) }}</textarea></div><div><label class="label">Location</label><input class="input" name="location" value="{{ old('location',$store->location) }}"></div></div><div class="space-y-4"><div><label class="label">Store Logo</label>@if($store->logo)<img src="{{ asset('storage/'.$store->logo) }}" class="mb-3 h-24 w-24 rounded-2xl object-cover">@endif<input class="input" type="file" name="logo" accept="image/jpeg,image/png,image/webp"></div><div><label class="label">Store Banner</label>@if($store->banner)<img src="{{ asset('storage/'.$store->banner) }}" class="mb-3 h-28 w-full rounded-xl object-cover">@endif<input class="input" type="file" name="banner" accept="image/jpeg,image/png,image/webp"></div></div></div><div class="mt-6 flex gap-3"><button class="btn-primary">Save Changes</button><a class="btn-outline" href="{{ route('shops.show',$store->slug) }}">Preview Store</a></div></form>@endsection
