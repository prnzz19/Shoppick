@extends('layouts.seller') @section('title','Products') @section('content')<div class="flex justify-between"><h1 class="text-2xl font-black">Products</h1><a class="btn-primary" href="{{ route('seller.products.create') }}">Add product</a></div><div class="card mt-6 overflow-x-auto"><table class="w-full text-left text-sm"><thead><tr class="border-b"><th class="p-4">Product</th><th>SKU</th><th>Price</th><th>Stock</th><th>Status</th><th></th></tr></thead><tbody>@foreach($products as $product)<tr class="border-b"><td class="p-4 font-semibold">{{ $product->name }}</td><td>{{ $product->sku }}</td><td>₱{{ number_format($product->salePrice(),2) }}</td><td>{{ $product->stock }}</td><td>{{ $product->is_active?'Active':'Hidden' }}</td><td><a href="{{ route('seller.products.edit',$product) }}" class="text-brand-600">Edit</a><form class="inline" method="POST" action="{{ route('seller.products.destroy',$product) }}">@csrf @method('DELETE') <button class="ml-2 text-rose-600">Archive</button></form></td></tr>@endforeach</tbody></table></div><div class="mt-4">{{ $products->links() }}</div>@endsection
