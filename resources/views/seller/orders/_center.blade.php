@extends('layouts.seller') @section('title','Orders') @section('content')<div><p class="text-sm font-semibold text-brand-600">Order Management</p><h1 class="text-2xl font-extrabold text-navy-900">Orders</h1></div>@php($tabs=[''=>'All','new'=>'New','processing'=>'Processing','to_ship'=>'To Ship','shipped'=>'Shipped','completed'=>'Completed','cancelled'=>'Cancelled'])<div class="mt-6 flex gap-5 overflow-x-auto border-b">@foreach($tabs as $key=>$label)<a href="{{ route('seller.orders.index',['status'=>$key]) }}" class="whitespace-nowrap border-b-2 px-1 pb-3 text-sm font-semibold {{ request('status','')===$key?'border-brand-500 text-brand-700':'border-transparent text-slate-500' }}">{{ $label }}</a>@endforeach</div><form class="mt-5 max-w-sm"><input type="hidden" name="status" value="{{ request('status') }}"><input class="input" name="q" value="{{ request('q') }}" placeholder="Search order or buyer"></form><div class="card mt-5 overflow-x-auto"><table class="w-full min-w-[760px] text-left text-sm"><thead><tr class="border-b text-xs uppercase text-slate-400"><th class="p-4">Order #</th><th>Buyer</th><th>Items</th><th>Total</th><th>Status</th><th>Date</th><th>Action</th></tr></thead><tbody>@forelse($orders as $order)<tr class="border-b"><td class="p-4 font-semibold">{{ $order->seller_order_number }}</td><td>{{ $order->order->buyer_name }}</td><td>{{ $order->items->count() }}</td><td>₱{{ number_format($order->subtotal,2) }}</td><td><x-admin.status-badge :status="$order->status"/></td><td>{{ $order->created_at->format('M d, Y') }}</td><td><a class="font-semibold text-brand-600" href="{{ route('seller.orders.show',$order) }}">View Order</a></td></tr>@empty<tr><td colspan="7" class="p-12 text-center"><p class="font-semibold">No orders</p><p class="text-sm text-slate-500">New orders will appear here.</p></td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $orders->links() }}</div>@endsection
