@extends('layouts.seller') @section('title','Settings') @section('content')<div><p class="text-sm font-semibold text-brand-600">Preferences</p><h1 class="text-2xl font-extrabold text-navy-900">Settings</h1></div><div x-data="{tab:'account'}" class="mt-6"><div class="flex gap-5 border-b">@foreach(['account'=>'Account','shipping'=>'Shipping','security'=>'Security'] as $key=>$label)<button @click="tab='{{ $key }}'" class="px-1 pb-3 text-sm font-semibold" :class="tab==='{{ $key }}'?'border-b-2 border-brand-500 text-brand-700':'text-slate-500'">{{ $label }}</button>@endforeach</div><form x-show="tab==='account'" method="POST" action="{{ route('seller.settings.account') }}" class="card mt-5 max-w-2xl space-y-4 p-6">@csrf @method('PUT')<h2 class="font-bold">Account</h2><input class="input" name="name" value="{{ auth()->user()->name }}" required><input class="input bg-slate-50" value="{{ auth()->user()->email }}" disabled><input class="input" name="phone" value="{{ auth()->user()->phone }}" placeholder="Phone"><button class="btn-primary">Save Changes</button></form><form x-show="tab==='shipping'" method="POST" action="{{ route('seller.settings.shipping') }}" class="card mt-5 max-w-2xl space-y-4 p-6">@csrf @method('PUT')<h2 class="font-bold">Shipping</h2><input class="input" type="number" min="0" step=".01" name="shipping_fee" value="{{ $setting->shipping_fee }}" required><input class="input" type="number" min="1" max="30" name="processing_days" value="{{ $setting->processing_days }}" required><label class="flex gap-2 text-sm"><input type="checkbox" name="cod_enabled" value="1" @checked($setting->cod_enabled)> Accept Cash on Delivery</label><button class="btn-primary">Save Shipping</button></form><form x-show="tab==='security'" method="POST" action="{{ route('seller.settings.password') }}" class="card mt-5 max-w-2xl space-y-4 p-6">@csrf @method('PUT')<h2 class="font-bold">Security</h2><input class="input" type="password" name="current_password" required placeholder="Current password"><input class="input" type="password" name="password" required placeholder="New password"><input class="input" type="password" name="password_confirmation" required placeholder="Confirm password"><button class="btn-primary">Change Password</button></form></div>@endsection
