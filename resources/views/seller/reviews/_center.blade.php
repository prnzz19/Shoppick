@extends('layouts.seller') @section('title','Reviews') @section('content')<div><p class="text-sm font-semibold text-brand-600">Customer Feedback</p><h1 class="text-2xl font-extrabold text-navy-900">Reviews</h1></div><div class="card mt-6 p-5"><span class="text-3xl font-black">{{ number_format($average,1) }}</span><span class="ml-2 text-sun-500">★★★★★</span></div><div class="mt-5 space-y-3">@forelse($reviews as $review)<div class="card p-5"><div class="flex justify-between"><div><b>{{ $review->product->name }}</b><p class="text-sm text-slate-500">{{ $review->user->name }} · {{ $review->created_at->format('M d, Y') }}</p></div><span class="text-sun-500">{{ str_repeat('★',$review->rating) }}</span></div><p class="mt-3 text-sm">{{ $review->comment ?: 'No written comment.' }}</p>@if($review->reply)<div class="mt-3 rounded-lg bg-slate-50 p-3 text-sm"><b>Your reply</b><p>{{ $review->reply->reply }}</p></div>@else<form method="POST" action="{{ route('seller.reviews.reply',$review) }}" class="mt-3 flex gap-2">@csrf<input class="input" name="reply" required placeholder="Reply to this review"><button class="btn-primary btn-sm">Reply</button></form>@endif</div>@empty<div class="card p-12 text-center"><p class="font-semibold">No Reviews</p><p class="text-sm text-slate-500">Customer reviews will appear here.</p></div>@endforelse</div><div class="mt-5">{{ $reviews->links() }}</div>@endsection
