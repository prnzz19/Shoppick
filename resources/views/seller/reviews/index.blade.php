@extends('layouts.seller') @section('title','Reviews') @section('content')<div><p class="text-sm font-semibold text-brand-600">Customer Feedback</p><h1 class="text-2xl font-extrabold text-navy-900">Product Reviews</h1></div><div class="card mt-6 p-5"><div class="flex items-center gap-3"><span class="text-4xl font-black text-navy-900">{{ number_format($average,1) }}</span><div><p class="text-sun-400">★★★★★</p><p class="text-xs text-slate-500">Average rating from verified purchases</p></div></div></div><div class="mt-5 space-y-4">@forelse($reviews as $review)<div class="card p-5"><div class="flex justify-between gap-4"><div><p class="font-bold">{{ $review->product->name }}</p><p class="text-sm text-slate-500">{{ $review->user->name }} · {{ $review->created_at->format('M d, Y') }}</p></div><span class="text-sun-400">{{ str_repeat('★',$review->rating) }}{{ str_repeat('☆',5-$review->rating) }}</span></div><p class="mt-3 text-sm">{{ $review->comment ?: 'No written comment.' }}</p></div>@empty<div class="card p-10 text-center text-slate-500">No reviews yet.</div>@endforelse</div><div class="mt-5">{{ $reviews->links() }}</div>@endsection
