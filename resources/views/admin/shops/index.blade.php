@extends('layouts.admin')
@section('title','Manage Shops')
@section('content')
@php
    $prefix=request()->routeIs('superadmin.*')?'superadmin':'admin';
    $tabs=[''=>'All','pending'=>'Pending','active'=>'Active','rejected'=>'Rejected','restricted'=>'Restricted','suspended'=>'Suspended','escalated'=>'Escalated'];
    $viewer=auth()->user(); $full=$viewer->isSuperAdmin(); $can=fn($permission)=>$full||$viewer->hasPermissionTo($permission);
@endphp
<div><p class="text-sm font-semibold text-brand-600">Marketplace</p><h1 class="text-2xl font-extrabold">Manage Shops</h1><p class="mt-1 text-sm text-slate-500">Review shared SHOPPICK Store records according to your assigned permissions.</p></div>
<div class="mt-6 flex gap-5 overflow-x-auto border-b">@foreach($tabs as $key=>$label)<a href="{{ route($prefix.'.shops.index',['status'=>$key]) }}" class="whitespace-nowrap border-b-2 px-1 pb-3 text-sm font-semibold {{ request('status','')===$key?'border-brand-500 text-brand-700':'border-transparent text-slate-500' }}">{{ $label }}</a>@endforeach</div>
<form class="card mt-5 grid gap-3 p-4 sm:grid-cols-4"><input type="hidden" name="status" value="{{ request('status') }}"><input class="input sm:col-span-2" name="q" value="{{ request('q') }}" placeholder="Search shop, seller, or email"><label class="flex items-center gap-2 text-sm"><input type="checkbox" name="reported" value="1" @checked(request('reported'))> Reported shops</label><label class="flex items-center gap-2 text-sm"><input type="checkbox" name="violations" value="1" @checked(request('violations'))> With violations</label><button class="btn-outline sm:col-span-4 sm:w-fit">Apply Filters</button></form>
<div class="card mt-5 overflow-x-auto"><table class="w-full min-w-[1000px] text-left text-sm"><thead><tr class="border-b"><th class="p-4">Shop</th><th>Seller</th><th>Products</th><th>Orders</th><th>Rating</th><th>Reports</th><th>Violations</th><th>Status</th><th>Joined</th><th></th></tr></thead><tbody>
@forelse($shops as $shop)@php($application=$shop->user->sellerApplications->sortByDesc('created_at')->first())<tr class="border-b"><td class="p-4"><div class="flex items-center gap-3">@if($shop->logo)<img src="{{ asset('storage/'.$shop->logo) }}" class="h-10 w-10 rounded-lg object-cover">@else<div class="flex h-10 w-10 items-center justify-center rounded-lg bg-brand-50 font-bold text-brand-600">{{ strtoupper(substr($shop->name,0,1)) }}</div>@endif<b>{{ $shop->name }}</b></div></td><td>{{ $shop->user->name }}<p class="text-xs text-slate-400">{{ $shop->user->email }}</p></td><td>{{ $shop->products_count }}</td><td>{{ $shop->seller_orders_count }}</td><td>{{ number_format($shop->rating_avg,1) }}</td><td>{{ $shop->reports_count }}</td><td>{{ $shop->violations_count }}</td><td><x-admin.status-badge :status="$application?->status==='escalated'?'escalated':$shop->status"/></td><td>{{ $shop->created_at->format('M d, Y') }}</td><td><div class="flex min-w-[210px] flex-wrap items-center gap-2"><a class="font-semibold text-brand-600" href="{{ route($prefix.'.shops.show',$shop) }}">{{ $application?->status==='escalated'?'Review':'View Shop' }}</a>@if(!$full&&$shop->status==='pending'&&$application?->status==='pending'&&$can('approve_shops'))<form method="POST" action="{{ route('admin.shops.status',$shop) }}" data-confirm-title="Approve this seller shop?" data-confirm-message="This will activate the Shop and allow the Seller to start selling on SHOPPICK." data-confirm-action="Approve Shop" data-confirm-type="success">@csrf<input type="hidden" name="action" value="approve"><button class="text-sm font-semibold text-leaf-600">Approve</button></form>@endif @if(!$full&&$shop->status==='pending'&&$application?->status==='pending'&&$can('reject_shops'))<form method="POST" action="{{ route('admin.shops.status',$shop) }}" data-confirm-title="Reject this seller application?" data-confirm-action="Reject Application" data-confirm-type="danger" data-confirm-reason="true">@csrf<input type="hidden" name="action" value="reject"><button class="text-sm font-semibold text-rose-600">Reject</button></form>@endif @if(!$full&&$shop->status==='pending'&&$application?->status==='pending'&&$can('review_shops'))<form method="POST" action="{{ route('admin.shops.escalate',$shop) }}" data-confirm-title="Escalate this application?" data-confirm-message="This application will be sent to Super Admin for further review." data-confirm-action="Escalate" data-confirm-type="warning" data-confirm-reason="true">@csrf<button class="text-sm font-semibold text-sun-600">Escalate</button></form>@endif @if($full&&$shop->status==='pending'&&$application?->status==='escalated')<form method="POST" action="{{ route('superadmin.shops.status',$shop) }}" data-confirm-title="Approve this escalated seller shop?" data-confirm-action="Approve Shop" data-confirm-type="success">@csrf<input type="hidden" name="action" value="approve"><button class="text-sm font-semibold text-leaf-600">Approve</button></form><form method="POST" action="{{ route('superadmin.shops.status',$shop) }}" data-confirm-title="Reject this escalated seller application?" data-confirm-action="Reject Application" data-confirm-type="danger" data-confirm-reason="true">@csrf<input type="hidden" name="action" value="reject"><button class="text-sm font-semibold text-rose-600">Reject</button></form>@endif</div></td></tr>
@empty<tr><td colspan="10" class="p-12 text-center text-slate-500">No shops match these filters.</td></tr>@endforelse
</tbody></table></div><div class="mt-5">{{ $shops->links() }}</div>
@endsection
