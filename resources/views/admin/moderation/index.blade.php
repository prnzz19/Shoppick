@extends('layouts.admin') @section('title','Moderation') @section('content') @php($prefix=request()->routeIs('superadmin.*')?'superadmin':'admin')<div><p class="text-sm font-semibold text-brand-600">Trust & Safety</p><h1 class="text-2xl font-extrabold">Product Image Moderation</h1><p class="text-sm text-slate-500">Automated detections require human review.</p></div>@php($tabs=[''=>'All','pending_scan'=>'Pending Review','flagged'=>'Flagged Products','approved'=>'Approved','rejected'=>'Rejected','scan_failed'=>'Scan Failed'])<div class="mt-6 flex gap-5 overflow-x-auto border-b">@foreach($tabs as $key=>$label)<a href="{{ route($prefix.'.moderation.index',['status'=>$key]) }}" class="whitespace-nowrap border-b-2 px-1 pb-3 text-sm font-semibold {{ request('status','')===$key?'border-brand-500 text-brand-700':'border-transparent text-slate-500' }}">{{ $label }}</a>@endforeach</div><div class="card mt-5 overflow-x-auto"><table class="w-full min-w-[850px] text-left text-sm"><thead><tr class="border-b"><th class="p-4">Image</th><th>Product</th><th>Seller / Shop</th><th>Detection</th><th>Confidence</th><th>Priority</th><th>Date</th><th>Status</th><th></th></tr></thead><tbody>@forelse($scans as $scan)<tr class="border-b"><td class="p-4"><img src="{{ $scan->image->url }}" class="h-14 w-14 rounded-lg object-cover"></td><td>{{ $scan->product->name }}</td><td>{{ $scan->seller?->name }}<p class="text-xs text-slate-400">{{ $scan->store?->name }}</p></td><td>{{ ucwords(str_replace('_',' ',$scan->detected_category??'No detection')) }}</td><td>{{ $scan->confidence!==null?number_format($scan->confidence*100,1).'%':'—' }}</td><td>{{ ucfirst($scan->risk_level) }}</td><td>{{ $scan->created_at->format('M d, Y') }}</td><td><x-admin.status-badge :status="$scan->status"/></td><td><a class="font-semibold text-brand-600" href="{{ route($prefix.'.moderation.show',$scan) }}">Review</a></td></tr>@empty<tr><td colspan="9" class="p-12 text-center">No moderation scans.</td></tr>@endforelse</tbody></table></div>{{ $scans->links() }}@endsection
