@extends('layouts.admin')
@section('title','Sellers')
@section('content')
<h1 class="text-2xl font-bold text-navy-900">Sellers</h1><p class="mt-2 text-slate-500">Approved sellers only. Pending and rejected applications appear in Seller Applications.</p><form class="my-5 flex gap-3"><input name="q" class="input" value="{{ request('q') }}" placeholder="Search seller, email or shop"><button class="btn-primary">Search</button></form>
<div class="card overflow-x-auto"><table class="w-full"><thead><tr>@foreach(['Seller','Shop','Products','Approved','Shop Status','Actions'] as $label)<th class="table-th">{{ $label }}</th>@endforeach</tr></thead><tbody>@forelse($sellers as $seller)<tr><td class="table-td">{{ $seller->name }}<p class="text-sm text-slate-500">{{ $seller->email }}</p><p class="text-xs text-brand-700">Buyer + Seller</p></td><td class="table-td">{{ $seller->store->name }}</td><td class="table-td">{{ $seller->store->products_count }}</td><td class="table-td">{{ $seller->sellerProfile->approved_at?->format('M d, Y') ?? 'Legacy approval' }}</td><td class="table-td"><x-admin.status-badge :status="$seller->store->status"/></td><td class="table-td"><a class="btn-outline btn-sm" href="{{ route('admin.sellers.show',$seller) }}">View Seller</a></td></tr>@empty<tr><td colspan="6" class="p-8 text-center">No approved sellers match.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $sellers->links() }}</div>
@endsection
