@extends('layouts.admin')
@section('title','Seller Applications')
@section('content')
@php $super=auth()->user()->isSuperAdmin(); @endphp
<div><h1 class="text-2xl font-black text-navy-900">Seller Applications</h1><p class="mt-1 text-sm text-slate-500">{{ $super?'Review applications escalated by Admin.':'Approve or reject normal applications, and escalate questionable cases.' }}</p></div>
<div class="mt-6 space-y-4">@forelse($applications as $application)<div class="card p-5"><div class="flex flex-wrap justify-between gap-4"><div><h2 class="font-bold">{{ $application->store_name }}</h2><p class="text-sm text-slate-500">{{ $application->user->name }} · {{ $application->user->email }}</p><p class="mt-2 text-sm">{{ $application->store_description }}</p><p class="mt-1 text-sm">{{ $application->phone }} · {{ $application->address }}</p></div><x-admin.status-badge :status="$application->status"/></div>
@if($application->status==='escalated')<div class="mt-4 rounded-xl border border-sun-200 bg-sun-50 p-4 text-sm"><p class="font-bold uppercase text-sun-700">Escalation</p><p>Escalated by {{ $application->escalator?->name }} · {{ $application->escalated_at?->format('M d, Y g:i A') }}</p><p class="mt-1">{{ $application->escalation_reason }}</p></div>@endif
@if(!$super && $application->status==='pending')<div class="mt-4 flex flex-wrap gap-2"><form method="POST" action="{{ route('admin.sellers.applications.review',$application) }}" class="flex flex-1 flex-wrap gap-2">@csrf<input name="review_notes" class="input flex-1" placeholder="Required when rejecting"><button name="status" value="approved" class="btn-primary">Approve</button><button name="status" value="rejected" class="btn-outline text-rose-600">Reject</button></form>@if(auth()->user()->hasPermissionTo('review_shops'))<form method="POST" action="{{ route('admin.sellers.applications.escalate',$application) }}" class="flex gap-2">@csrf<input name="reason" class="input" required placeholder="Escalation reason"><button class="btn-outline text-sun-600">Escalate</button></form>@endif</div>
@elseif($super && $application->status==='escalated')<form method="POST" action="{{ route('superadmin.sellers.applications.review',$application) }}" class="mt-4 flex flex-wrap gap-2">@csrf<input name="review_notes" class="input flex-1" placeholder="Required when rejecting"><button name="status" value="approved" class="btn-primary">Approve</button><button name="status" value="rejected" class="btn-outline text-rose-600">Reject</button></form>
@elseif($super && $application->status==='pending')<p class="mt-4 text-sm font-semibold text-sun-600">Pending Admin review.</p>@endif
</div>@empty<div class="card p-8 text-center text-slate-500">No seller applications found.</div>@endforelse</div><div class="mt-5">{{ $applications->links() }}</div>
@endsection
