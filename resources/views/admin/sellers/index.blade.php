@extends('layouts.admin')
@section('title','Seller Applications')
@section('content')
<h1 class="text-2xl font-bold text-navy-900">Seller Applications</h1><p class="mt-2 text-slate-600">Review requests from existing Buyer accounts. Approved shops are managed under Sellers.</p>
<form class="card my-5 flex flex-wrap gap-3 p-4"><input class="input flex-1" name="q" value="{{ request('q') }}" placeholder="Applicant, email or shop name"><select class="input !w-auto" name="status"><option value="">All statuses</option>@foreach(\App\Models\SellerApplication::STATUSES as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ str($status)->headline() }}</option>@endforeach</select><button class="btn-primary">Filter</button></form>
<div class="card overflow-x-auto"><table class="w-full"><thead><tr>@foreach(['Applicant','Shop','Submitted','Status','Last Updated','Action'] as $label)<th class="table-th">{{ $label }}</th>@endforeach</tr></thead><tbody>@forelse($applications as $application)<tr><td class="table-td">{{ $application->user?->name ?? 'Unavailable account' }}<p class="text-sm text-slate-500">{{ $application->user?->email }}</p></td><td class="table-td">{{ $application->store_name }}</td><td class="table-td">{{ $application->created_at->format('M d, Y') }}</td><td class="table-td"><x-admin.status-badge :status="$application->status"/></td><td class="table-td">{{ $application->updated_at->format('M d, Y') }}</td><td class="table-td"><a class="btn-outline btn-sm" href="{{ route('admin.sellers.applications.show',$application) }}">View / Review &rarr;</a></td></tr>@empty<tr><td colspan="6" class="p-8 text-center text-slate-500">No applications match your filters.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $applications->links() }}</div>
@endsection
