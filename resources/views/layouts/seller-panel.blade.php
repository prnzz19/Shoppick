<!DOCTYPE html><html lang="{{ str_replace('_','-',app()->getLocale()) }}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="csrf-token" content="{{ csrf_token() }}"><title>@yield('title','Seller Center') · SHOPPICK</title>@vite(['resources/css/app.css','resources/js/app.js'])</head>
<body class="min-h-screen bg-slate-100">@php($store=auth()->user()->store) @php($nav=[['seller.dashboard','Dashboard'],['seller.products.index','Products'],['seller.inventory.index','Inventory'],['seller.orders.index','Orders'],['seller.sales.index','Sales'],['seller.reviews.index','Reviews'],['seller.store.edit','Store Settings']])
<div class="flex min-h-screen"><aside class="fixed inset-y-0 left-0 z-30 hidden w-64 flex-col bg-navy-900 lg:flex"><a href="{{ route('seller.dashboard') }}" class="flex items-center gap-3 px-5 py-5"><x-shoppick.logo class="h-10 w-10"/><div><span class="text-lg font-extrabold"><span class="text-brand-400">SHOP</span><span class="text-accent-400">PICK</span></span><p class="text-[10px] font-semibold uppercase tracking-[.2em] text-slate-400">Seller Center</p></div></a><div class="mx-3 rounded-xl border border-white/10 bg-white/5 p-3"><p class="truncate text-sm font-bold text-white">{{ $store->name }}</p><p class="truncate text-xs text-slate-400">{{ $store->location ?: 'SHOPPICK Seller' }}</p></div><nav class="flex-1 space-y-1 px-3 py-5"><p class="px-3 pb-2 text-[11px] font-semibold uppercase tracking-wider text-slate-500">Store Management</p>@foreach($nav as [$route,$label])<a href="{{ route($route) }}" class="flex items-center rounded-lg px-3 py-2.5 text-sm font-medium {{ request()->routeIs($route)||request()->routeIs(str_replace('.index','.*',$route))?'bg-brand-500/20 text-brand-300':'text-slate-300 hover:bg-white/5 hover:text-white' }}">{{ $label }}</a>@endforeach<p class="px-3 pb-2 pt-5 text-[11px] font-semibold uppercase tracking-wider text-slate-500">Marketplace</p><a href="{{ route('shops.show',$store->slug) }}" class="block rounded-lg px-3 py-2.5 text-sm text-slate-300 hover:bg-white/5">View My Store</a><a href="{{ route('home') }}" class="block rounded-lg px-3 py-2.5 text-sm text-slate-300 hover:bg-white/5">Storefront</a></nav><div class="border-t border-white/10 p-4"><p class="truncate text-sm font-semibold text-white">{{ auth()->user()->name }}</p><p class="text-xs text-slate-400">Seller</p><form method="POST" action="{{ route('logout') }}" class="mt-3">@csrf<button class="btn-sm w-full bg-rose-500/20 text-rose-300">Logout</button></form></div></aside>
<header class="fixed inset-x-0 top-0 z-30 flex items-center justify-between border-b bg-white px-4 py-3 lg:hidden"><a href="{{ route('seller.dashboard') }}" class="flex items-center gap-2"><x-shoppick.logo class="h-8 w-8"/><b><span class="text-brand-500">SHOP</span><span class="text-accent-500">PICK</span></b></a><button onclick="document.getElementById('seller-mobile-nav').classList.toggle('hidden')" class="p-2">☰</button></header><div id="seller-mobile-nav" class="fixed inset-x-0 top-14 z-30 hidden bg-navy-900 p-4 lg:hidden"><nav class="grid grid-cols-2 gap-2">@foreach($nav as [$route,$label])<a href="{{ route($route) }}" class="rounded-lg px-3 py-2 text-sm text-white">{{ $label }}</a>@endforeach</nav></div>
<main class="min-w-0 flex-1 px-4 pb-8 pt-20 lg:pl-72 lg:pr-8 lg:pt-7">@if(session('success'))<div class="alert-success mb-4">{{ session('success') }}</div>@endif @if(session('error'))<div class="alert-error mb-4">{{ session('error') }}</div>@endif @if($errors->any())<div class="alert-error mb-4">{{ $errors->first() }}</div>@endif @yield('content')</main></div>@stack('scripts')</body></html>
