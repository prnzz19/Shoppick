@extends('layouts.admin') @section('title','Manage Shops') @section('content')<div><p class="text-sm font-semibold text-brand-600">Marketplace</p><h1 class="text-2xl font-extrabold">Manage Shops</h1></div>@php($tabs=[''=>'All','active'=>'Active','pending'=>'Pending','restricted'=>'Restricted','suspended'=>'Suspended'])<div class="mt-6 flex gap-5 border-b">@foreach($tabs as $key=>$label)<a href="{{ route('superadmin.shops.index',['status'=>$key]) }}" class="border-b-2 px-1 pb-3 text-sm font-semibold {{ request('status','')===$key?'border-brand-500 text-brand-700':'border-transparent text-slate-500' }}">{{ $label }}</a>@endforeach</div><form class="mt-5 max-w-md"><input type="hidden" name="status" value="{{ request('status') }}"><input class="input" name="q" value="{{ request('q') }}" placeholder="Search shop, seller, or email"></form><div class="card mt-5 overflow-x-auto"><table class="w-full min-w-[950px] text-left text-sm"><thead><tr class="border-b"><th class="p-4">Shop</th><th>Seller</th><th>Products</th><th>Orders</th><th>Rating</th><th>Reports</th><th>Violations</th><th>Status</th><th>Joined</th><th></th></tr></thead><tbody>@foreach($shops as $shop)<tr class="border-b"><td class="p-4"><div class="flex items-center gap-3">@if($shop->logo)<img src="{{ asset('storage/'.$shop->logo) }}" class="h-10 w-10 rounded-lg object-cover">@endif<b>{{ $shop->name }}</b></div></td><td>{{ $shop->user->name }}<p class="text-xs text-slate-400">{{ $shop->user->email }}</p></td><td>{{ $shop->products_count }}</td><td>{{ $shop->seller_orders_count }}</td><td>{{ number_format($shop->rating_avg,1) }}</td><td>{{ $shop->reports_count }}</td><td>{{ $shop->violations_count }}</td><td><x-admin.status-badge :status="$shop->status"/></td><td>{{ $shop->created_at->format('M d, Y') }}</td><td><a class="font-semibold text-brand-600" href="{{ route('superadmin.shops.show',$shop) }}">Manage</a></td></tr>@endforeach</tbody></table></div>{{ $shops->links() }}@endsection
